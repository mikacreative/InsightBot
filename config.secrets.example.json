{
    "wecom": {
        "cid": "${WECOM_CID}",
        "secret": "${WECOM_SECRET}",
        "aid": "${WECOM_AID}"
    },
    "ai": {
        "api_key": "${AI_API_KEY}",
        "api_url": "${AI_API_URL}",
        "model": "${AI_MODEL}"
    }
}
