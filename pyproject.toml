[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "marketing-bot"
version = "0.3.0"
description = "AI-powered marketing insight bot"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["insightbot*"]
